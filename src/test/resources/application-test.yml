spring:
  datasource:
    url: jdbc:h2:mem:incident_management_test;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: false

jwt:
  secret: test-only-secret-key-not-used-anywhere-real-32bytes
  expiration: 3600000

groq:
  api:
    key: ""
